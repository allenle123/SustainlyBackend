export function calculateSustainabilityScore(productData: any): number {
  // TODO: Replace with actual ML model logic
  // Currently using a placeholder random score generator
  return Math.floor(Math.random() * 101);
}

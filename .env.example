# Sustainly Backend Environment Variables

# Google Gemini API Key - Required for AI-powered sustainability scoring with web search
# Note: For web search functionality, you need:
#  1. A Google Cloud project with billing enabled
#  2. Generative Language API enabled in your project
#  3. An API key with access to Gemini 1.5 models
GEMINI_API_KEY=your_gemini_api_key_here
PROJECT_ID=your_google_cloud_project_id

# Web Search Configuration (optional)
# Controls when the model uses web search to enhance responses
# Range: 0.0 (always use web search) to 1.0 (rarely use web search)
# Default is 0.7 if not specified
# Recommended value: 0.5 based on observed behavior in production
GEMINI_SEARCH_THRESHOLD=0.5

# AWS DynamoDB Configuration - Used for product cache
AWS_REGION=us-east-1
PRODUCT_TABLE_NAME=sustainly-product-cache

# Supabase Configuration - Used for user history tracking
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_key

# External API Configuration (if using real API instead of mock data)
USE_MOCK_DATA=false
CANOPY_API_KEY=your_canopy_api_key
